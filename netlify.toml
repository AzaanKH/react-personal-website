[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Optional: Add environment variable hints
[build.environment]
  NODE_VERSION = "18"